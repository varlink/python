[build-system]
requires = [
    "setuptools >= 48",
    "setuptools_scm[toml]",
    "setuptools_scm_git_archive",
    "wheel >= 0.29.0",
]
build-backend = 'setuptools.build_meta'

[tool.setuptools_scm]
local_scheme = "no-local-version"
